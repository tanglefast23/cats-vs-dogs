// Intentionally empty. This file is the future narrow bridge (via contextBridge)
// for desktop-only capabilities such as versioned file saves and window
// preferences. Nothing is exposed to the renderer until a feature needs it —
// the game must keep running with zero desktop APIs available.
